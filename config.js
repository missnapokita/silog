/* Public frontend configuration only. Never place API keys or admin secrets here. */
window.BTK_VOUCHER_CONFIG = Object.freeze({
  setupMode: true,
  apiBaseUrl: "",
  shortLinkUrl: "",
  tutorialUrl: "",
  voucherLifetimeMinutes: 15,
  requestTimeoutMs: 12000,
  pageVersion: "1.0.0"
});
